{
  "name": "word-gen",
  "version": "1.0.0",
  "description": "Random Word(s) Generator",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "node prebuild.js",
    "prettier": "prettier . --write --ignore-path .prettierignore"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "prettier": "3.4.2"
  }
}
